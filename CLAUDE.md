# Project rules

## Costs catalog in README

When adding, updating, or removing a unit cost (in `src/db/seed.ts` or via the API), **always update the "Unit costs catalog" table in `README.md`** to keep it in sync.
